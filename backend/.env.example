PORT=5000
# Supabase Auth + PostgreSQL (Phase 3H). MongoDB/mongoose/legacy-JWT are retired.
# NEVER expose SUPABASE_SECRET_KEY / DATABASE_URL to the frontend. These are read
# from environment only (see .gitignore: .env committed never).
SUPABASE_URL=https://tudkepbppaucnspxpfbg.supabase.co
SUPABASE_SECRET_KEY=replace_with_env_or_service_role_key
DATABASE_URL=postgres://replace_with_pg_connection_string
ML_SERVICE_URL=http://localhost:8000
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
AI_PROVIDER=gemini
FRONTEND_URL=http://localhost:5173
